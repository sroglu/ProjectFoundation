# Profiling
Terse `using`-scoped wrapper over `ProfilerMarker` so a profiled region reads as one allocation-free indented block.

**Key classes:** `ProfilerMarkers`, `ProfilerScope`
**Assembly:** `PFound.Utilities.Profiling`
**Tier:** engine
**Depends on:** none
