using PFound.NetworkLayer;
using PFound.ServerOperation.Core;

// TODO: point [NetworkOp] at YOUR real banded opcode enums — replace NetDomain.Sample with your
// per-game NetDomain value and SampleOp.#SCRIPTNAME# with your per-domain op value. Until then this
// line will not compile (the placeholder enums are intentionally fictitious).
[NetworkOp(NetDomain.Sample, SampleOp.#SCRIPTNAME#)]
public partial class #SCRIPTNAME#
{
    [Request(0)] public int  ExampleField;    // TODO: real request fields (index = wire key, append-only)
    [Reply(0)]   public long ExampleResult;   // TODO: real reply fields
}

// The source generator emits #SCRIPTNAME#.RequestMessage / #SCRIPTNAME#.ReplyMessage from the partial above.
public sealed class #SCRIPTNAME#Operation
    : ServerOperation<#SCRIPTNAME#.RequestMessage, #SCRIPTNAME#.ReplyMessage, ServerOperationResult>
{
    public #SCRIPTNAME#Operation(
        ServerOperationContext<#SCRIPTNAME#.RequestMessage, #SCRIPTNAME#.ReplyMessage, ServerOperationResult> context)
        : base(context) { }

    protected override ServerOperationResult PreCheck()          => throw new System.NotImplementedException(); // TODO: local pre-check
    protected override #SCRIPTNAME#.RequestMessage BuildRequest() => throw new System.NotImplementedException(); // TODO: build the request
    protected override ServerOperationResult Interpret(#SCRIPTNAME#.ReplyMessage reply) => throw new System.NotImplementedException(); // TODO: map reply to result
    protected override void ApplySuccess(ServerOperationResult result) { }                                       // TODO: apply authoritative state
}
