# ProjectFoundation

A modular Unity framework — reusable infrastructure for game projects.

Namespace prefix: `PFound.*`

## Status

Bootstrapping. Modules are added as they are built and verified.

## Layout

| Path | Purpose |
|------|---------|
| `Assets/` | Unity project / framework modules (added incrementally) |
</content>
